/**
* Represents the state of the item editor
* This interface includes properties for the editor item, selected tab, and payload.
* Change this intierface depending on your needs 
*/
export interface ItemEditorState  {
  message?: string;
}